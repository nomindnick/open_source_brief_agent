# Copy to .env (gitignored). Only needed if `hf` CLI auth is via env, or for future API keys.

# HuggingFace token — only needed if `hf auth login` hasn't been run on this machine.
# `hf papers list/read` needs read scope.
# HF_TOKEN=
