"""``ModelInterface`` and ``ModelResponse``.

Reasoning content is a first-class field on the response, separated from
agent-facing ``content`` so the parser never sees thinking tokens.

Filled in by Sprint 1.2.
"""
