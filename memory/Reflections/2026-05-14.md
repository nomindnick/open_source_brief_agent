Yesterday was heavy on agentic RL and small-model distillation — five keepers split between RL-for-agents (SDAR, ActGuide), agent memory (STALE), and inference efficiency (BEAM, training-free merging). The agentic-RL papers had concrete numbers (e.g. BEAM's 2.5× decoding speedup, training-free merging hitting 86.9% on GPQA) which felt high-signal.

Memory-validity work (STALE) was thinner than its abstract suggested — worth a closer look if more memory-validity papers appear.

For tomorrow: lean *slightly* away from another round of agentic-RL self-distillation papers unless the angle is genuinely new. Up-weight inference-systems work (KV cache, quantization, vLLM integrations) — those got short shrift yesterday despite being a stated high interest.
